# Browser Agent 商业场景扩展与架构解耦需求文档（BRD）

## Changelog

- 2026-07-19：增加线上客户交付形态和商业化上线门禁，首期采用“云端控制面 + 客户本机 Worker”，先完成账号租户、数据安全和生产部署，再进入受邀客户封闭内测。
- 2026-07-19：按单一 Browser Agent 项目现状更新，统一以 `main` 为开发主线，以 extension、adapter 和 policy 隔离业务能力。
- 2026-06-27：首次梳理版权检索取证与海外社交媒体自动化运营方向。

## 文档信息

- **日期**：2026-06-27
- **状态**：执行中
- **相关方**：产研团队、业务运营团队

## 1. 背景与业务契机

前期的复杂网页自动化实践验证了：通过 Browser Agent 模拟真实用户在浏览器中的观察和操作，可以覆盖传统接口或规则型爬虫难以处理的动态页面、登录态流程和人机协作场景。

这套能力的商业价值不仅是“读取网页”，更在于完成可审计的网页任务闭环。当前优先探索两个方向：

1. **版权侵权检索与取证**：面向短剧等内容版权方，自动发现疑似盗用页面，保存来源 URL、截图、页面信息和执行轨迹，形成可复核的线索报告。
2. **泛内容自动化运营**：面向 Reddit、X、YouTube、TikTok、Instagram 等平台，在明确授权与平台规则边界内执行内容发布、账号维护和运营辅助任务。

## 2. 核心架构解耦策略

业务场景增加后，不能把平台规则、版权判断或社媒发布流程直接塞进浏览器运行时。仓库统一采用单一主线和模块化扩展机制。

### 2.1 单一主线

- `main` 是唯一长期开发主线，保存可运行、可验证的 Browser Agent 平台。
- 日常功能可使用短期分支开发，验收后合入 `main` 并删除，不维护平行产品分支。
- README、部署脚本和交接文档以 `main` 的现状为准，避免出现不同分支对应不同产品或环境的假设。

### 2.2 模块隔离

- **底座层**：设备绑定、任务调度、Playwright 浏览器运行时、checkpoint、artifact、manual action、trace 和通用 API。
- **扩展层**：使用 Worker extension 和 adapter 注册具体能力，例如 `generic.browser_agent`、社媒上传和微信资料同步。
- **策略层**：通过 policy template、allowed domains、人工确认和审计字段约束具体任务，不把平台特例写入通用执行器。
- **收益**：底座只维护稳定的执行协议；具体业务可以独立测试、按需启用，并在失败时提供清晰的错误与人工接管边界。

## 3. 重点业务场景定义

### 3.1 场景一：短剧版权侵权检索（首个商业化 PoC）

- **用户痛点**：短剧上线后可能在小型网站或视频平台被盗用，人工巡检成本高且容易漏网。
- **任务输入**：作品名、别名、版权方、目标站点或允许域名、检索关键词和时间范围。
- **执行过程**：Browser Agent 检索目标页面，记录 URL、时间、页面标题、匹配线索和关键 DOM 信息。
- **取证输出**：页面截图、执行 trace、结构化线索和可下载报告；所有结果必须允许人工复核，不能把自动匹配直接表述为法律结论。
- **产品形态**：面向版权方的 B 端监测与线索管理服务。

### 3.2 场景二：海外社交媒体自动化运营

- **用户痛点**：多平台内容发布和账号维护需要大量重复操作，且各平台 API 能力与限制不一致。
- **Agent 方案**：复用真实浏览器 profile，按平台 adapter 执行导航、上传、填写元数据、可见性设置和提交。
- **安全边界**：默认不绕过验证码或平台风控；遇到登录失效、验证码、敏感发布动作时创建 `manual_action`，由人工确认后继续。
- **审计要求**：记录任务发起人、目标账号、内容、可见性、关键步骤、最终结果和 artifact，不保存第三方账号密码、Cookie 或 Session 明文。

### 3.3 线上客户交付形态

- **首期形态**：云端提供 Web、Admin、Gateway、API、MySQL、Redis 和对象存储；客户本机安装 Worker，控制本机 Chrome/Chromium。
- **数据边界**：第三方平台登录态、Cookie、Session 和浏览器 profile 默认保留在客户设备；云端只保存任务控制数据、经授权上传的 artifact 和审计记录。
- **网络边界**：Worker 仅通过出站 HTTPS 访问云端，不要求客户开放入站端口；Admin 与客户 Web 使用独立入口和权限模型。
- **客户隔离**：设备、任务、run、manual action、trace、artifact 和报告必须绑定租户，并在所有列表、详情、下载和操作接口执行 resource ownership 校验。
- **发布策略**：先面向少量受邀客户封闭内测，在鉴权、租户隔离、生产部署、备份恢复、监控告警和 Worker 安装升级通过验收前，不开放公共注册。
- **延后范围**：完全云端 Browser Worker、第三方平台会话云托管和大规模浏览器集群不属于首期目标。

## 4. 下一步行动计划

1. **建立账号与租户边界**：接入登录、tenant、membership 和 RBAC，为设备及所有 Automation 资源强制 resource ownership，并补跨租户越权测试。
2. **建立生产数据与部署基线**：接入托管 MySQL、Redis、对象存储和 Secret Manager，统一 staging/production、TLS、migration、备份恢复、不可变镜像和回滚流程。
3. **产品化本机 Worker**：提供 Windows 安装包、系统凭据存储、开机启动、版本兼容、受控升级、撤销和日志导出。
4. **补齐安全与运维门禁**：移除通配 allowed domains，增加内网地址阻断、访问频率、配额、人工确认、数据留存/删除、集中日志、指标和告警。
5. **完成版权检索业务建模与真实 PoC**：定义作品、关键词、目标站点、候选线索和取证 artifact 模型，跑通“检索 → 截图取证 → Admin 复核 → 导出结果”。
6. **稳定现有 adapter 并进入封闭内测**：补社媒/微信 fixture、错误码、登录状态、重试和人工接管验证，只邀请明确授权的少量客户使用。

当前底座、Windows 本地运行和 Browser Agent 确定性端到端验收已经完成；下一阶段不再规划新的长期业务分支，工作重点转向生产化上线门禁与版权检索 PoC。详细实施方案见 `docs/tech/0719-browser-agent-productionization-plan.md`。
