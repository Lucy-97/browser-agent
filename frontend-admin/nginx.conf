server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API proxy could go here, but Gateway handles it, 
    # and Admin app points to API directly or via ingress.
}
