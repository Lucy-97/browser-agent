# BrowserAgent Web 前端环境变量（复制为 .env.local）
#
# 这些以 NEXT_PUBLIC_ 开头的变量会被打进浏览器端 bundle，切勿放任何密钥。

# 后端网关地址（apiClient 请求基址）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# OAuth2 SSO（可选；不配则 apiClient 不会跳转 SSO）
NEXT_PUBLIC_SSO_BASE_URL=
NEXT_PUBLIC_SSO_CLIENT_ID=
